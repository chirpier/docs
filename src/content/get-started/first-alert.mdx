# Create Your First Alert

Once a chart is working, create a policy for the same event definition.

Start with a simple threshold so you can verify the full loop:

1. emit data
2. evaluate the policy
3. trigger an alert
4. inspect the alert in the inbox
5. verify delivery to a destination
