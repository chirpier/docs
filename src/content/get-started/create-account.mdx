# Create Account and Get API Key

Create a ChirrOp account at [chirrop.com](https://www.chirrop.com/auth), then open the Integration page in the web app to copy your API key.

That API key is the credential used by the SDKs and the logs API.

Treat it like any other production secret.
