# Terminology

- `event`: the metric or signal name sent to Chirpier
- `event definition`: the saved definition associated with an event name
- `policy`: the rule Chirpier evaluates for an event
- `alert`: the record created when a policy triggers
- `connector`: the delivery target for alert notifications
- `embed token`: the short-lived token used to render a hosted chart embed
