# Event Definitions

When Chirpier receives a new event name, it creates an event definition that becomes the basis for charts, rollups, and policies.

Use event definitions to:

- set human-friendly titles
- choose sensible default aggregates
- classify the meaning of an event
- keep teams aligned on naming

Good event definitions make the web app easier to navigate and reduce duplicate chart setup work.
