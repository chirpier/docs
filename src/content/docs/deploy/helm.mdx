# Helm and Charts

The `charts` repo should remain the source of truth for install details, values, and release notes.

This page should explain when to use the Helm chart, the high-level deployment flow, and where to find the authoritative package docs.
